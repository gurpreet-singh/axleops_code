package com.fleetmanagement.csvimport.model;

/**
 * Supported data types for import field validation.
 */
public enum ImportDataType {
    STRING,
    INTEGER,
    LONG,
    DOUBLE,
    BOOLEAN,
    DATE,
    ENUM,
    EMAIL,
    PHONE,
    PAN,
    GSTIN
}
