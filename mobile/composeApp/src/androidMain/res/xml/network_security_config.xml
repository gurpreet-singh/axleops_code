<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for AxleOps debug builds.
  Allows cleartext (HTTP) traffic to local development servers:
  - 10.0.2.2 = Android emulator alias for host machine's localhost
  - localhost / 127.0.0.1 = direct loopback (physical device via adb reverse)

  In production, remove this config or restrict to HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.4</domain>
    </domain-config>
</network-security-config>
