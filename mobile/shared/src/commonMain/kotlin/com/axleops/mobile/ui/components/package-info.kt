/**
 * AxleOps Shared Component Catalog.
 *
 * This package contains reusable UI components shared across all roles.
 * Components consume design tokens from [com.axleops.mobile.ui.theme]
 * and are composed into feature screens.
 *
 * Sub-packages:
 * - `buttons/`  — Primary, Secondary, Text, Destructive, Icon buttons
 * - `cards/`    — Standard card, summary card
 * - `badges/`   — Trip phase badge, exception badge, count badge
 * - `inputs/`   — Text field, dropdown, validation helpers
 * - `feedback/` — Confirmation dialog, snackbar, upload card
 * - `sheets/`   — Bottom sheet wrapper
 * - `stepper/`  — Milestone stepper (horizontal + vertical)
 *
 * Role-specific components live in their role's package
 * (e.g., `ui/driver/`), NOT here.
 *
 * @see docs/design/mobile-design-system.md §2 Component Catalog
 */
package com.axleops.mobile.ui.components
